/**
 * SearchScheduleSelector Component
 *
 * A weekly schedule selector for split-lease property searches.
 * Allows users to select 2-5 contiguous nights per week with
 * drag-and-drop functionality.
 */

export { SearchScheduleSelector } from './SearchScheduleSelector';
export type {
  Day,
  Listing,
  SearchScheduleSelectorProps,
  ValidationResult,
  ListingCount,
} from './types';
